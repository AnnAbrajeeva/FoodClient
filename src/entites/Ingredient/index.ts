import { IngredientApi, IngredientModel } from './entity';

import { normalizeIngredient } from './normalizer';
import { getProducts } from './utils';

export { getProducts, type IngredientApi, type IngredientModel, normalizeIngredient };
